1、使用库函数strlen
2、int get_string_length(char* str)
{
	char* head = str;
	char* bottom = str;
	while (*bottom != '\0')
	{
		bottom++;
	}
	return bottom - head;
}
3、int get_string_length(char* str)
{
	int count=0;
	while (*str!= '\0')
	{
		str++;
    count++;
	}
	return count;
}
